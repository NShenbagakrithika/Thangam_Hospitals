import type {Metadata} from "next";import Link from "next/link";import {PageHero} from "@/components/page-hero";import {services} from "@/data/site";
export const metadata:Metadata={title:"Specialities",description:"Explore women’s health, paediatrics, elderly care, home healthcare, general medicine and emergency care at Thangam Hospitals.",alternates:{canonical:"/specialities"}};
export default function Specialities(){return <><PageHero eyebrow="Specialities" title="Clear pathways to the care you need" intro="Start with the concern that best matches your needs. Our team can help guide you to the appropriate next step."/><section className="py-20"><div className="container-pad space-y-5">{services.map(({slug,title,short,icon:Icon})=><article id={slug} className="card scroll-mt-32 grid gap-5 p-7 sm:grid-cols-[auto_1fr_auto] sm:items-center" key={slug}><span className="grid size-14 place-items-center rounded-full bg-gold/15 text-gold"><Icon size={28}/></span><div><h2 className="font-serif text-3xl text-navy">{title}</h2><p className="mt-2 max-w-3xl leading-7 text-muted">{short}</p></div><Link className="btn-secondary" href={slug==="home-healthcare"?"/home-healthcare":"/contact#appointment"}>Enquire now</Link></article>)}</div></section></>}
