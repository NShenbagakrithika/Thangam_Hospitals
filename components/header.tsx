"use client";
import Link from "next/link";import {useState} from "react";import {CalendarDays,Menu,Phone,X} from "lucide-react";import {navigation} from "@/data/site";import {Logo} from "./logo";
export function Header(){const [open,setOpen]=useState(false);return <><a href="#main" className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[100] focus:bg-white focus:p-3">Skip to content</a><header className="sticky top-0 z-50 border-b border-navy/10 bg-white/95 backdrop-blur"><div className="bg-navy text-white"><div className="container-pad flex min-h-10 items-center justify-between gap-4 text-sm"><span>Kandigai, Chennai</span><Link href="/contact#appointment" className="font-bold text-gold hover:text-white">Appointment enquiry</Link></div></div><div className="container-pad flex min-h-20 items-center justify-between"><Logo/><nav className="hidden items-center gap-7 lg:flex" aria-label="Main navigation">{navigation.map(n=><Link key={n.href} href={n.href} className="font-semibold text-navy hover:text-gold">{n.label}</Link>)}<Link className="btn-primary" href="/contact#appointment"><CalendarDays size={19}/>Book Appointment</Link></nav><button onClick={()=>setOpen(!open)} className="grid size-12 place-items-center rounded-md border border-navy/15 lg:hidden" aria-expanded={open} aria-controls="mobile-menu" aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<nav id="mobile-menu" className="container-pad border-t border-navy/10 pb-5 lg:hidden" aria-label="Mobile navigation">{navigation.map(n=><Link onClick={()=>setOpen(false)} key={n.href} href={n.href} className="block border-b border-navy/10 py-4 text-lg font-semibold">{n.label}</Link>)}<Link onClick={()=>setOpen(false)} className="btn-primary mt-4 w-full" href="/contact#appointment">Book Appointment</Link></nav>}</header></>}
